{
  "_comment": "internOS — Claude Code hooks. Merge these entries into ~/.claude/settings.json (user-global) or your project's .claude/settings.json. The Stop hook records the session in the active workstream's SESSIONS.md, if any. If pwd is not inside a workstream, the script silently no-ops.",
  "hooks": {
    "Stop": [
      {
        "matcher": "",
        "hooks": [
          {
            "type": "command",
            "command": "~/.claude/skills/intern-os/scripts/log-session.sh"
          }
        ]
      }
    ]
  }
}
