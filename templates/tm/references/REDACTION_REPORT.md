# Redaction Report — <TM_NAME>

Scope reviewed: `workspaces/<WORKSPACE>/projects/<PROJECT><WORKSTREAM_PATH_SUFFIX>/`

Reviewer: <AUTHOR>
Date: <ISO8601>

## Checked

- Secrets and tokens (env files, auth.json, credentials)
- Lock / PID / runtime files
- Generated bytecode and caches
- Nested git repositories under `code/**`
- Global agent identity files (`SOUL.md`, `config.yaml`, harness-specific identity)
- Unrelated projects and workspaces
- Session logs and memories

## Excluded (enforced by scripts/verify_tm.sh)

- `code/**`
- `.tick/lock`, `.tick/session.json`
- `**/__pycache__/**`, `**/*.pyc`
- `**/.env`, `**/.env.*`
- `**/auth.json`
- `**/*.lock`, `**/*.pid`
- `**/.internos-warnings`
- Global identity files outside the canonical_path

## Sensitive-adjacent content still present

Document anything that *is* in the payload and that a receiver should treat with care. Examples:

- Human role context for project stakeholders.
- Communication-surface references (Slack/Discord/Telegram channel IDs without tokens).
- Repository URLs in `RESOURCES.md`.
- Client-facing documents and proposals.

This TM is intended for **<AUDIENCE>** (private / authorized collaboration / public release / etc.). Adjust handling accordingly.

## What to do if you spot a leak

If a receiver finds something that *should* have been redacted:

1. Stop using the TM.
2. Notify the author with the file path and the kind of leak.
3. Author re-exports with the leak fixed and a bumped version.
4. Receiver verifies the new TM and resumes work.

Do **not** silently scrub the receiver's local copy and continue — that masks the source-side error.
